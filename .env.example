# Leave empty during UI-only development to enable clearly marked demo authentication.
# Set this before deploying against the API, for example: VITE_API_URL=https://api.example.edu.et/api
VITE_API_URL=
